<UserControl x:Class="TriviaClientGUI.JoinRoomView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TriviaClientGUI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <!-- Grid with two rows -->
        <Grid>
            <TextBlock Text="Select Room" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="85,31,0,0" Height="35" Width="222" FontSize="25" TextDecorations="Underline"/>
            <!-- Empty border for player names -->
            <Border Width="300" Height="300" HorizontalAlignment="Center" VerticalAlignment="Center" BorderBrush="Black" BorderThickness="1">
                <!-- StackPanel to host buttons for player names -->
                <StackPanel x:Name="roomNamesStackPanel">

                </StackPanel>
            </Border>

            <StackPanel Margin="429,78,-119,130">
                <TextBlock x:Name="userInRoomTitle" Text="Players in room:" HorizontalAlignment="Center" TextAlignment="Center" Margin="0,10,0,0" Height="35" Width="222" FontSize="17" TextDecorations="Underline" Visibility="Hidden"/>

                <Border x:Name ="userInRoomBorder" Width="150" Height="150" HorizontalAlignment="Center" VerticalAlignment="Center" BorderBrush="Black" BorderThickness="1" Visibility="Hidden">
                    <!-- StackPanel to host buttons for player names -->
                    <StackPanel x:Name="userInRoomStackPanel" Margin="10,0,10,0" Visibility="Hidden">

                    </StackPanel>
                </Border>

                <Button x:Name="JoinRoomButton" Content="Join Room" HorizontalAlignment="Center" VerticalAlignment="Top" Click="CloseRoom_Click" Margin="0,10,0,0" Height="30" Width="120" FontSize="17" Visibility="Hidden"/>
            </StackPanel>
            
            <!-- "Return to Menu" button -->
            <Button Content="Return to Menu" HorizontalAlignment="Center" VerticalAlignment="Top" Click="CloseRoom_Click" Margin="0,394,0,0" Height="30" Width="120" FontSize="17"/>
        </Grid>
    </Grid>
</UserControl>
